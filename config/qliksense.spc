  connection "qliksense" {
    plugin = "qliksense"

    # The Qliksense host instance (required).
    #host    = "YOUR_ZENDESK_SUBDOMAIN_NAME" # e.g. server.example.com

    # Engine service port 
    #engine_port      = 4242

    #Proxy service port (required).
    #proxy_port      = 4343

    # cert_client = "/path/to/client.pem"
    # cert_client_key = "/path/to/client_key.pem"
    # cert_ca = "/path/to/root.pem"
  }
